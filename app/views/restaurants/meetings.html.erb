<div class='d-flex flex-column align-items-center'>
  <h1>Meetings open to join at <%= @restaurant.name %></h1>
  <div class='cards'>
    <% @meetings.each do |meeting| %>
      <div class = 'card-meeting p-3 my-1'>
        <div class="card-meeting-text">
          <h3><%= meeting.user.username %></h3>
          <%= link_to cl_image_tag(meeting.user.photo), user_path(meeting.user), class: "avatar-link-large" %>
          <p><%= meeting.restaurant.address %></p>
          <p><%= meeting.meet_time.strftime("%H:%M")%></p>
        </div>

        <div class='d-flex align-items-center flex-column'>
          <%= link_to 'Meeting Details', meeting_path(meeting), class: 'btn btn-info' %>
        </div>
      </div>
    <% end %>
  </div>
  <%= link_to 'Back to map', restaurants_path, class: 'btn btn-info' %>
</div>
