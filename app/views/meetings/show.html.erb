<div class='d-flex flex-column align-items-center'>
<h1>Meeting</h1>
  <div>
    <%= @meeting.restaurant.name %>
  </div>
  <div
  id="map"
  style="width: 100%;
  height: 400px;"
  data-markers="<%= @marker.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
></div>
  <br>
  <div>
    <%= link_to 'Meetings list', meetings_path, class: 'btn btn-info' %>
  </div>
</div>
